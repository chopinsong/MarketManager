<?xml version="1.0" encoding="utf-8"?>
<swipe.SwipeItemLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/goods_edit_swipe_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/left_menu_goods_edit"
        android:layout_width="120dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:layout_marginBottom="3dp"
        android:layout_marginTop="3dp"
        android:background="@color/bg_blue"
        android:gravity="center"
        android:text="@string/edit_text"
        android:textColor="@color/white" />

    <TextView
        android:id="@+id/right_menu_goods_edit"
        android:layout_width="120dp"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:layout_marginBottom="3dp"
        android:layout_marginTop="3dp"
        android:background="#FF2520"
        android:gravity="center"
        android:text="@string/del_text"
        android:textColor="@color/white" />

    <include layout="@layout/goods_edit_item_content" />

</swipe.SwipeItemLayout>